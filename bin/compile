#!/usr/bin/env bash
set -o errexit
set -o nounset

URL="https://nodejs.org/dist/v4.2.3/node-v4.2.3-linux-x64.tar.gz"
TMP=node

mkdir -P $TMP
curl $URL -s -o - | tar xzf - -C $TMP
chmod +x $TMP/bin/*

PATH=$TMP/bin:$PATH

cd $1/src/server/
npm install